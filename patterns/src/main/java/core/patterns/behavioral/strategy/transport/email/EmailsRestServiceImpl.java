package core.patterns.behavioral.strategy.transport.email;

import core.patterns.behavioral.strategy.model.EmailRecord;

public class EmailsRestServiceImpl implements EmailsRestService {

    @Override
    public void send(EmailRecord value) {
        //mock implementation
    }
}
