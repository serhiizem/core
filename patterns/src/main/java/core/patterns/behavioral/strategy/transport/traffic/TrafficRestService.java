package core.patterns.behavioral.strategy.transport.traffic;

import core.patterns.behavioral.strategy.model.TrafficRecord;
import core.patterns.behavioral.strategy.transport.RestService;

public interface TrafficRestService extends RestService<TrafficRecord> {
}
