package core.patterns.behavioral.strategy.transport.email;

import core.patterns.behavioral.strategy.model.EmailRecord;
import core.patterns.behavioral.strategy.transport.RestService;

public interface EmailsRestService extends RestService<EmailRecord> {
}
