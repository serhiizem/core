package core.patterns.behavioral.strategy.transport.traffic;

import core.patterns.behavioral.strategy.model.TrafficRecord;

public class TrafficRestServiceImpl implements TrafficRestService {

    @Override
    public void send(TrafficRecord value) {
        //mock implementation
    }
}
