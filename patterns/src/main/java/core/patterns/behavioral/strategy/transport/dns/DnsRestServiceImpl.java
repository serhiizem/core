package core.patterns.behavioral.strategy.transport.dns;

import core.patterns.behavioral.strategy.model.DnsRecord;

public class DnsRestServiceImpl implements DnsRestService {

    @Override
    public void send(DnsRecord value) {
        //mock implementation
    }
}
