package core.patterns.behavioral.strategy.transport.dns;

import core.patterns.behavioral.strategy.model.DnsRecord;
import core.patterns.behavioral.strategy.transport.RestService;

public interface DnsRestService extends RestService<DnsRecord> {
}
